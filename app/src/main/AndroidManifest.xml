<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.mojumob.instagram">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.CadastroActivity" />
        <activity android:name=".ui.activity.ComentariosActivity" />
        <activity android:name=".ui.activity.EditarPerfilActivity" />
        <activity android:name=".ui.activity.FiltroActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.PerfilAmigoActivity" />
        <activity android:name=".ui.activity.VisualizarPostagemActivity"></activity>
    </application>

</manifest>